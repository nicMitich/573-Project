export default function ChromaPage() {
  return (
    <div className="page">
      <p className="page-label">03 — Vector DB</p>
      <h1 className="page-title">Vector Search</h1>
      <p className="page-desc">
        Upload a resume or enter a search query to find semantically matching jobs
        using ChromaDB embeddings.
      </p>

      <hr className="divider" />

      <div className="placeholder">
        ChromaDB search interface — implementation coming soon
      </div>
    </div>
  )
}
